{
  "name": "myblog",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "docs:dev": "vuepress dev docs",
    "docs:build": "vuepress build docs",
    "notes:pull": "cd ./docs/notes && git pull",
    "dist:auto-deploy": "npm run notes:pull && npm run docs:build && cd ./dist && git pull && git add * && git commit -m deploy && git push origin master"
  },
  "author": "me",
  "license": "ISC",
  "devDependencies": {
    "vuepress": "^1.8.2"
  },
  "dependencies": {
    "swiper": "^6.8.4"
  }
}